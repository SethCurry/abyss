## Docker

Dockerfiles are in `build/docker`.  You can build them by running `task build-docker`.

## Code Style

### Comments

Comments should be brief and should only explain why the function exists and a very brief summary of what it does.
